@model ICollection<Employee>
@{
    ViewData["Title"] = "Index";
}

<section id="configuration">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <div class="card-title-wrap bar-success">
                        <h4 class="card-title text-center">معلومات الموظفين</h4>
                    </div>
                </div>
                <div class="card-body collapse show">
                    <div class="card-block card-dashboard">
                     
                            <div class="row"><div class="col-sm-12 col-md-3">
                                <div class="dataTables_length" id="DataTables_Table_0_length">
                                 <label>عرض 
@*                                  asp-for="EmployeeId" asp-items="@(new SelectList(Model.EmployeesList, nameof(Employee.Id), nameof(Employee.FullName)))
*@                                 <select name="DataTables_Table_0_length" aria-controls="DataTables_Table_0" class="form-control form-control-sm">
                                    <option value="10">10</option>
                                    <option value="25">25</option>
                                    <option value="50">50</option>
                                    <option value="100">100</option>
                                 </select>
                                </label>
                                </div>
                                </div>
                                      <div class="col-lg-1"></div> 
                                        <div class="col-sm-12 col-md-3">
                                            <div id="DataTables_Table_0_filter" class="dataTables_filter">
                                                <label>بحث:<input type="search" class="form-control form-control-sm" placeholder="" aria-controls="DataTables_Table_0"></label></div></div>
                                <div class="col-lg-1"></div>
                                <div class="col-lg-3">
                                    <button id="addRow" class="btn btn-primary mb-2"> 
                                        <i class="ft-plus"></i>&nbsp;
                                        <a asp-controller="HR" asp-action="index" target="_blank"> إضافة مستخدم جديد</a>
                                   </button>
                                </div>
                            </div> 
                        <!------------------------------------------- start table of HRS-------------------------------------->   
                        <table class="table table-striped table-bordered" >
                            <thead>
                                <tr>
                                    <th>الاسم</th>
                                    <th>رقم التعريف</th>
                                    <th>التليفون</th>
                                    <th>تاريخ الميلاد </th>
                                    <th>النوع</th>
                                    <th>تاريخ التعاقد</th>
                                    <th>المرتب</th>
                                    <th>وقت الحضور</th>
                                    <th>وقت الانصراف</th>
                                    <th>الجنسيه</th>
                                    <th>تعديل</th>
                                    <th> تعديل اعدادات الموظف</th>
                                    <th>حذف</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    @foreach(var item in Model)
                                    {
                                        <td>@item.empName</td>
                                        <td>@item.id</td>
                                        <td>@item.EmpPhones</td>
                                        <td>@item.empDateOfBirth</td>
                                        <td>@item.empGender</td>
                                        <td>@item.empHireDate</td>
                                        <td>@item.empNetSalary</td>
                                        <td>@item.requiredDepartureTime</td>
                                        <td>@item.Nationality</td>
                                        <td><a class="btn btn-success mb-2" asp-controller="Employee" asp-action="Edit" asp-route-id="@item.id">تعديل</a></td>
                                        <td><a class="btn btn-info mb-2" asp-controller= "Employee" asp-action="Editsettings" asp-route-id="@item.id">تعديل الاعدادات</a>  </td>
                                        <td><a class="btn btn-danger mb-2" asp-controller="Employee" asp-action="delete" asp-route-id="@item.id">حذف</a></td>
                                    <td>
                                        <button class="btn btn-success mb-2">
                                            <a asp-controller="HR" asp-action="Index" asp-route-id="@*item.id*@">تعديل</a>
                                        </button>
                                    </td>
                                     <td>
                                        <button class="btn btn-info mb-2">
                                            <a asp-controller= " GeneralSetting"asp-action="Edit" asp-route-id="@*@item.id*@">تعديل الاعدادات</a>
                                        </button>
                                    </td>
                                    <td>
                                        <button class="btn btn-danger mb-2">
                                            <a asp-controller="HR" asp-action="Index" asp-route-id="@*@item.id*@">حذف</a>
                                        </button>
                                    </td>
               
                                    
                                    }
                                </tr>                                                                                                                                                                                                                     </yr>
                                       
                                                                                                                                                                                                                                                                                                               </yr>
                            </tbody>
                        </table>
                        </div>
                        <!------------------------------------------- end table of Empolyees-------------------------------------->
                    </div>
                </div>
            </div>
        </div>

</section>



     